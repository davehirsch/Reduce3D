// ===========================================================================//	CFloatArray.h// ===========================================================================#pragma once#include "fp.h"#include <LArray.h>// ---------------------------------------------------------------------------//		¥ CFloatArray// ---------------------------------------------------------------------------class	CFloatArray : public LArray {	public:		class		ArrayIOErr{};					CFloatArray(						LComparator		*inComparator = nil,						Boolean			inKeepSorted = false);					~CFloatArray();		float		Pop();		void		Push(float *inNum);		void		Clear();		float		operator[](short inSub);		void		Update(short index, float inNum);};